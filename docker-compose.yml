services:
  # =========================================
  # FRONTEND (Bootstrap via npm + routes.js)
  # =========================================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: node:20-alpine
    container_name: app_frontend
    working_dir: /app
    volumes:
      - ./frontend:/app
      - ./frontend/node_modules:/app/node_modules
    ports:
      - "3000:3000"
    environment:
      - CHOKIDAR_USEPOLLING=true
      - REACT_APP_API_URL=http://localhost:8000/api
    command: sh -c "npm install && npm run build && npm run dev"
    depends_on:
      - backend
    networks:
      - vite_et_gourmand_network

  # =========================================
  # BACKEND (API Symfony)
  # =========================================
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: app_backend
    working_dir: /var/www/html
    volumes:
      - ./backend/src:/var/www/html
      - ./backend/vendor:/var/www/html/vendor
    environment:
      DATABASE_URL: "mysql://symfony:symfony@mysql:3306/vite_et_gourmand_db?serverVersion=8.0"
      MONGODB_URL: "mongodb://symfony:symfony@mongodb:27017/vite_et_gourmand_db?authSource=admin"
      MONGODB_DB: "vite_et_gourmand_mongo_db"
    depends_on:
      - mysql
      - mongodb
    networks:
      - vite_et_gourmand_network

  # =========================================
  # NGINX (Reverse Proxy pour le  Backend)
  # =========================================
  nginx-backend:
    image: nginx:1.27-alpine
    container_name: vite_et_gourmand_nginx
    ports:
      - "8000:80"
    volumes:
      - ./backend:/var/www/html
      - ./backend/docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - backend
      - php
    networks:
      - vite_et_gourmand_network

  php:
    build:
      context: ./backend/docker/php
      dockerfile: Dockerfile
    container_name: php_app
    volumes:
      - ./backend/src:/var/www/html
    environment:
      MONGODB_URL: "mongodb://symfony:symfony@mongodb:27017/vite_et_gourmand_db?authSource=admin"
      MONGODB_DB: "vite_et_gourmand_db"
      DATABASE_URL: "mysql://symfony:symfony@mysql:3306/vite_et_gourmand_db?serverVersion=8.0"
      APP_ENV: dev
      # Autorise le frontend (port 3000) à appeler l'API (CORS)
      CORS_ALLOW_ORIGIN: "http://localhost:3000"
    depends_on:
      - mongodb
      - mysql
    networks:
      - app_net

  # --- MongoDB ---
  mongodb:
    image: mongo:7
    container_name: app_mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: symfony
      MONGO_INITDB_ROOT_PASSWORD: symfony
      MONGO_INITDB_DATABASE: vite_et_gourmand_db
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - app_net

  mongo-express:
    image: mongo-express:latest
    container_name: app_mongo_express
    restart: unless-stopped
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: symfony
      ME_CONFIG_MONGODB_ADMINPASSWORD: symfony
      ME_CONFIG_MONGODB_SERVER: mongodb
      ME_CONFIG_BASICAUTH_USERNAME: admin
      ME_CONFIG_BASICAUTH_PASSWORD: admin
    ports:
      - "8081:8081"
    depends_on:
      - mongodb
    networks:
      - app_net

  # --- MySQL ---
  mysql:
    image: mysql:8.0
    container_name: app_mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: vite_et_gourmand_db
      MYSQL_USER: symfony
      MYSQL_PASSWORD: symfony
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - app_net

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: app_phpmyadmin
    environment:
      PMA_HOST: mysql
      PMA_USER: symfony
      PMA_PASSWORD: symfony
    ports:
      - "8082:80"
    depends_on:
      - mysql
    networks:
      - app_net

networks:
  app_net:
    driver: bridge

volumes:
  mongo_data:
  db_data: